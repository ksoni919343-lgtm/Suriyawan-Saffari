'use client';
import { useEffect } from 'react';

export default function GooglePicker({ onPick }) {
  useEffect(() => {
    // Load Google Picker API
  }, []);

  return <button onClick={() => {}}>Pick File</button>;
}
